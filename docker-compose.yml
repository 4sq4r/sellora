version: '3'

services:
  postgres:
    image: postgres:17
    environment:
      - POSTGRES_USER=sellora
      - POSTGRES_PASSWORD=sellora
      - POSTGRES_DB=sellora
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
